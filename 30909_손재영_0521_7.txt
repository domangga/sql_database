데이터정의(DDL) : 객체를 새로

CREATE문
형식:
CREATE TABLE소유계정, 테이블이름(
열1 이름 자료형,
열1 이름 자료형,
열1 이름 자료형,
열1 이름 자료형,
...
열N 이름 자료형);

숫자 NUMBER(3)
문자 타입 VARCHAR2(5)

테이블 이름 생성규칙
1.테이블 이름은 문자로 시자행 한다 한글도 가능하면 숫자로 시작할 수 없음
에티블 이름은 30byte이하여야 한다 ㅡㅈㄱ 영어30자 한글은 15자까지 사영가능
같은 사영자 소유의 테이블 이름은 중복될수 없다
테이블 이름은 영문자 한글가능, 숫자 와 특수문자를 사영할 수 있다
SQL 키워드 테이블이름으로 사용할 숭 없다 함수 이름 select from 등은 테이블 이름으로 사용불가

열이름 생성 규칙
열 이름은 문자로 시작해야 한다
열 이름은 30byte 이하여야 한다
한테이블의 열이름은 중복될 수 없다
열이름은 영문자 한글가능 숫자와 특수문자를 사용할수있다
SQL 키워드 테이블이름으로 사용할 숭 없다 함수 이름 select from 등은 테이블 이름으로 사용불가

문제34 >'성적' 테이블을 생성하고
성명 
국어
영어
구학
그래픽디자인
DB
프로그래밍
평가
의 열응 생성하시오
(단 성명과 평가는 문자형 타입선언,나머지는숫자형 3자리 선언하시오)

create table 성적표(
성명 varchar2(8),
국어 number(3),
영어 number(3),
수학 number(3),
그래픽디자인 number(3),
DB number(3),
프로그래밍 number(3),
평가 varchar2(5));

insert